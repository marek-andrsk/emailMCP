# Pinned exactly: an unpinned SDK is how production drifted ahead of local and
# broke on a field the SDK had added (AccessToken.claims). Bump deliberately,
# then run the suite.
mcp==1.29.0
anyio==4.12.1
imapclient==3.1.0
html2text==2025.4.15
pillow==12.0.0
python-dotenv==1.2.2
uvicorn==0.41.0
